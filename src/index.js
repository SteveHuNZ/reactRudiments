import React from "react";
import ReactDOM from "react-dom";
import './index.css'

const list = ( < h1 className = "title" >
        william and kaitlyn 同的的 的 < /h1>)


        //rendering elements 
        ReactDOM.render(list, document.getElementById('root'))